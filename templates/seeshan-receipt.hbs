<!DOCTYPE html>
<html>
<head>
    <style>
        body { 
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
        }
        .container { 
            max-width: 800px;
            margin: 0 auto;
            background-color: #ffffff;
        }
        .header-image {
            width: 100%;
            height: 112px;
            object-fit: cover;
        }
        .footer-image {
            width: 100%;
            height: 112px;
            object-fit: cover;
        }
        .content {
            padding: 32px;
        }
        .receipt-details { 
            margin: 20px 0;
        }
        .receipt-row { 
            display: flex;
            margin: 12px 0;
            padding: 8px 0;
        }
        .receipt-label { 
            width: 200px;
            font-weight: bold;
            color: #374151;
        }
        .receipt-value {
            flex: 1;
            color: #1f2937;
        }
        .meta-info {
            text-align: left;
            margin-bottom: 24px;
        }
        .meta-info p {
            margin: 4px 0;
            font-size: 14px;
        }
        .meta-info strong {
            color: #374151;
        }
        .signature-note {
            text-align: right;
            padding-right: 64px;
            margin-bottom: -40px;
            position: relative;
            z-index: 1;
        }
        .signature-note p {
            text-align: left;
            font-size: 10px;
            color: #666;
            display: inline-block;
        }
        .receipt-message {
            background-color: #f3f4f6;
            padding: 16px;
            text-align: center;
            font-style: italic;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <img 
            src="https://ctmihnkvxjdkkyryphvm.supabase.co/storage/v1/object/public/organization-images//SeeshanHeader.png"
            alt="Seeshan Header" 
            class="header-image"
        />
        
        <div class="content">
            <div class="meta-info">
                <p><strong>Receipt No:</strong> {{donation.receipt_no}}</p>
                <p><strong>Date:</strong> {{donation.date}}</p>
            </div>

            <div class="receipt-details">
                <div class="receipt-row">
                    <span class="receipt-label">Received From:</span>
                    <span class="receipt-value">{{donation.donor.donor_name}}</span>
                </div>
                <div class="receipt-row">
                    <span class="receipt-label">City:</span>
                    <span class="receipt-value">{{donation.donor.city}}</span>
                </div>
                <div class="receipt-row">
                    <span class="receipt-label">Amount:</span>
                    <span class="receipt-value">Rs. {{donation.amount}}</span>
                </div>
                <div class="receipt-row">
                    <span class="receipt-label">Purpose:</span>
                    <span class="receipt-value">{{donation.purpose}}</span>
                </div>
            </div>

            <div class="signature-note">
                <p>
                    Signature is not required<br/>
                    as the document is to be digitally signed
                </p>
            </div>
        </div>

        <img 
            src="https://ctmihnkvxjdkkyryphvm.supabase.co/storage/v1/object/public/organization-images//SeeshanFooter.png"
            alt="Seeshan Footer" 
            class="footer-image"
        />

        <div class="receipt-message">
            {{{receiptMessage}}}
        </div>
    </div>
</body>
</html>